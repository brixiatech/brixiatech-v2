---
// BlackoutHyperGlass.astro — Pagina-prodotto (Opzione C, prodotto-bandiera DLC)
// Struttura: hero prodotto (asimmetrico, sinistra) → 4 blocchi corpo a due colonne
//            (titolo sx / testo dx) → CTA finale.
// Il blocco "edge" (distintivo) ha fondo --c-onyx + bordo copper: signature = il
//            prodotto che "si scurisce". I blocchi NON sono una sequenza → niente indici 01–04.
// lang/t ricavati internamente (mai props). Link cross-page lang-aware verso la home.
// Token: --c-* / --space-* / --font-* da global.css. ZERO immagini (da aggiungere poi).
import { getLangFromUrl, useTranslations } from "../i18n/utils";
const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

// Base URL lang-aware per i link cross-page verso la home (prefixDefaultLocale: false).
const base = lang === "en" ? "/en/" : "/";
const linkHow = `${base}#how-eyebrow`;
const linkBlogic = `${base}#blogic-eyebrow`;

// Blocchi corpo a due colonne. "edge" è il blocco distintivo (variante visiva).
const blocks = [
  { key: "what", variant: "plain", link: null },
  { key: "edge", variant: "feature", link: null },
  { key: "how", variant: "plain", link: linkHow, linkKey: "prod.bhg.how.link" },
  { key: "scale", variant: "plain", link: linkBlogic, linkKey: "prod.bhg.scale.link" },
] as const;
---
<article class="bhg">
  <!-- HERO PRODOTTO — asimmetrico, allineato a sinistra -->
  <header class="bhg-hero" aria-labelledby="bhg-name">
    <div class="bhg-hero__inner">
      <p class="bhg-hero__eyebrow">{t("prod.bhg.eyebrow")}</p>
      <h1 class="bhg-hero__name" id="bhg-name">{t("prod.bhg.name")}</h1>
      <p class="bhg-hero__tagline">{t("prod.bhg.tagline")}</p>
    </div>
  </header>

  <!-- BLOCCHI CORPO — due colonne (titolo sx / testo dx) -->
  <div class="bhg-body">
    {blocks.map((b) => (
      <section class={`bhg-block bhg-block--${b.variant}`}>
        <div class="bhg-block__inner">
          <div class="bhg-block__head">
            <h2 class="bhg-block__title">{t(`prod.bhg.${b.key}.title`)}</h2>
          </div>
          <div class="bhg-block__content">
            <p class="bhg-block__body">{t(`prod.bhg.${b.key}.body`)}</p>
            {b.link && (
              <a class="bhg-block__link" href={b.link}>
                {t(b.linkKey)}<span class="bhg-block__arrow" aria-hidden="true">&#8594;</span>
              </a>
            )}
          </div>
        </div>
      </section>
    ))}
  </div>

  <!-- CTA FINALE -->
  <section class="bhg-cta" aria-labelledby="bhg-cta-title">
    <div class="bhg-cta__inner">
      <h2 class="bhg-cta__title" id="bhg-cta-title">{t("prod.bhg.cta.title")}</h2>
      <p class="bhg-cta__body">{t("prod.bhg.cta.body")}</p>
      <a class="bhg-cta__button" href="#contact-eyebrow">{t("prod.bhg.cta.button")}</a>
    </div>
  </section>
</article>

<style>
  .bhg {
    display: block;
  }

  /* ---------- HERO PRODOTTO ---------- */
  .bhg-hero {
    padding: calc(var(--space-section-y-lg) + 40px) var(--space-section-x) 32px;
  }
  .bhg-hero__inner {
    max-width: var(--container-max);
    margin: 0 auto;
  }
  .bhg-hero__eyebrow {
    display: inline-block;
    margin: 0;
    padding: 7px 14px;
    border: 1px solid var(--c-accent-glow);
    border-radius: var(--radius-pill);
    background: rgba(10, 18, 38, 0.6);
    backdrop-filter: blur(8px);
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--c-mist-bright);
  }
  .bhg-hero__name {
    margin: 28px 0 0;
    max-width: 16ch;
    font-family: var(--font-display);
    font-size: clamp(2.6rem, 6vw, 4.6rem);
    line-height: 1.04;
    font-weight: 600;
    letter-spacing: -0.02em;
    color: var(--c-pearl);
  }
  .bhg-hero__tagline {
    margin: 24px 0 0;
    max-width: 46ch;
    font-size: clamp(1.05rem, 1.6vw, 1.3rem);
    line-height: 1.5;
    color: var(--c-mist);
  }

  /* ---------- BLOCCHI CORPO (due colonne) ---------- */
  .bhg-block {
    padding: var(--space-section-y) var(--space-section-x);
  }
  .bhg-block__inner {
    max-width: var(--container-max);
    margin: 0 auto;
    display: grid;
    grid-template-columns: minmax(0, 5fr) minmax(0, 7fr);
    column-gap: 64px;
    align-items: start;
  }
  .bhg-block__title {
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 2.6vw, 2.1rem);
    line-height: 1.22;
    font-weight: 500;
    color: var(--c-pearl);
  }
  .bhg-block__body {
    margin: 0;
    font-size: 1rem;
    line-height: 1.7;
    color: var(--c-mist);
  }
  .bhg-block__link {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    margin-top: 24px;
    font-family: var(--font-mono);
    font-size: 13px;
    letter-spacing: 0.04em;
    color: var(--c-cyan);
    text-decoration: none;
    transition: color 0.2s var(--ease);
  }
  .bhg-block__arrow {
    transition: transform 0.2s var(--ease);
  }
  .bhg-block__link:hover {
    color: var(--c-accent-bright);
  }
  .bhg-block__link:hover .bhg-block__arrow {
    transform: translateX(4px);
  }
  .bhg-block__link:focus-visible {
    outline: 2px solid var(--c-cyan);
    outline-offset: 4px;
    border-radius: 2px;
  }

  /* ---------- SIGNATURE: blocco distintivo "edge" ---------- */
  /* Il prodotto che si scurisce: fondo più profondo, filo rame. */
  .bhg-block--feature .bhg-block__inner {
    box-sizing: border-box;
    background: var(--c-onyx);
    border: 1px solid var(--border-hairline);
    border-left: 2px solid var(--c-copper);
    border-radius: var(--radius-card);
    padding: 48px;
    column-gap: 56px;
  }
  .bhg-block--feature .bhg-block__title {
    color: var(--c-pearl);
  }

  /* ---------- CTA FINALE ---------- */
  .bhg-cta {
    padding: var(--space-section-y) var(--space-section-x) var(--space-section-y-lg);
  }
  .bhg-cta__inner {
    max-width: 720px;
    margin: 0 auto;
    text-align: center;
  }
  .bhg-cta__title {
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(1.6rem, 3vw, 2.4rem);
    line-height: 1.2;
    font-weight: 500;
    color: var(--c-pearl);
  }
  .bhg-cta__body {
    margin: 16px auto 0;
    max-width: 52ch;
    color: var(--c-mist);
    line-height: 1.6;
  }
  .bhg-cta__button {
    display: inline-block;
    margin-top: 32px;
    padding: 14px 32px;
    border: 1px solid var(--c-cyan);
    border-radius: var(--radius-card);
    background: transparent;
    font-family: var(--font-mono);
    font-size: 13px;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--c-cyan);
    text-decoration: none;
    transition: background 0.2s var(--ease), color 0.2s var(--ease);
  }
  .bhg-cta__button:hover {
    background: var(--c-cyan);
    color: var(--c-void);
  }
  .bhg-cta__button:focus-visible {
    outline: 2px solid var(--c-cyan);
    outline-offset: 4px;
  }

  /* ---------- RESPONSIVE ---------- */
  @media (max-width: 900px) {
    .bhg-hero {
      padding: calc(var(--space-section-y) + 24px) 24px var(--space-section-y);
    }
    .bhg-block {
      padding: var(--space-section-y) 24px;
    }
    .bhg-block__inner {
      grid-template-columns: 1fr;
      row-gap: 20px;
    }
    .bhg-block--feature .bhg-block__inner {
      padding: 32px 24px;
    }
  }
  @media (max-width: 560px) {
    .bhg-hero {
      padding: 72px 24px 56px;
    }
    .bhg-block {
      padding: 56px 24px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .bhg-block__link,
    .bhg-block__arrow,
    .bhg-cta__button {
      transition: none;
    }
  }
</style>
