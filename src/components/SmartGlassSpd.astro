---
// SmartGlassSpd.astro — Pagina-prodotto Smart Glass SPD (oscuramento variabile/continuo).
// Struttura: hero prodotto (asimmetrico, sinistra) → 4 blocchi corpo a due colonne
//            (titolo sx / testo dx) → CTA finale. Derivato da SmartFilmPnlc.astro.
// Il blocco "edge" (distintivo) ha fondo --c-onyx + bordo copper: signature = il
//            dimming continuo, l'unico vetro della gamma regolabile come un dimmer;
//            chiude con la nota onesta (SPD non è privacy).
// Differenza dal template PNLC: il blocco "how" NON ha link (il meccanismo SPD a
//            particelle sospese è diverso dallo switchable a cristalli liquidi).
//            Il blocco "what" linka alla pagina PDLC (logica a stati) → const pdlcHref.
// lang/t ricavati internamente (mai props). Token: --c-* / --space-* / --font-* da global.css.
import { getLangFromUrl, useTranslations } from "../i18n/utils";
const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

// Base URL lang-aware per i link cross-page verso la home (prefixDefaultLocale: false).
const base = lang === "en" ? "/en/" : "/";
const linkBlogic = `${base}#blogic-eyebrow`;

// Link a pagina-prodotto: i segmenti di path sono tradotti → const dedicato lang-aware.
const pdlcHref = lang === "en"
  ? "/en/technologies/smart-film-pdlc"
  : "/tecnologie/smart-film-pdlc";

// Blocchi corpo a due colonne. "edge" è il blocco distintivo (variante visiva).
// "how" senza link: il meccanismo SPD (particelle sospese) è diverso dallo switchable.
const blocks = [
  { key: "what", variant: "plain", link: pdlcHref, linkKey: "prod.spd.what.link" },
  { key: "edge", variant: "feature", link: null },
  { key: "how", variant: "plain", link: null },
  { key: "scale", variant: "plain", link: linkBlogic, linkKey: "prod.spd.scale.link" },
] as const;
---
<article class="spd">
  <!-- HERO PRODOTTO — asimmetrico, allineato a sinistra -->
  <header class="spd-hero" aria-labelledby="spd-name">
    <div class="spd-hero__inner">
      <p class="spd-hero__eyebrow">{t("prod.spd.eyebrow")}</p>
      <h1 class="spd-hero__name" id="spd-name">{t("prod.spd.name")}</h1>
      <p class="spd-hero__tagline">{t("prod.spd.tagline")}</p>
    </div>
  </header>

  <!-- BLOCCHI CORPO — due colonne (titolo sx / testo dx) -->
  <div class="spd-body">
    {blocks.map((b) => (
      <section class={`spd-block spd-block--${b.variant}`}>
        <div class="spd-block__inner">
          <div class="spd-block__head">
            <h2 class="spd-block__title">{t(`prod.spd.${b.key}.title`)}</h2>
          </div>
          <div class="spd-block__content">
            <p class="spd-block__body">{t(`prod.spd.${b.key}.body`)}</p>
            {b.link && (
              <a class="spd-block__link" href={b.link}>
                {t(b.linkKey)}<span class="spd-block__arrow" aria-hidden="true">&#8594;</span>
              </a>
            )}
          </div>
        </div>
      </section>
    ))}
  </div>

  <!-- CTA FINALE -->
  <section class="spd-cta" aria-labelledby="spd-cta-title">
    <div class="spd-cta__inner">
      <h2 class="spd-cta__title" id="spd-cta-title">{t("prod.spd.cta.title")}</h2>
      <p class="spd-cta__body">{t("prod.spd.cta.body")}</p>
      <a class="spd-cta__button" href={`${base}#contact-eyebrow`}>{t("prod.spd.cta.button")}</a>
    </div>
  </section>
</article>

<style>
  .spd {
    display: block;
  }

  /* ---------- HERO PRODOTTO ---------- */
  .spd-hero {
    padding: calc(var(--space-section-y-lg) + 40px) var(--space-section-x) 32px;
  }
  .spd-hero__inner {
    max-width: var(--container-max);
    margin: 0 auto;
  }
  .spd-hero__eyebrow {
    display: inline-block;
    margin: 0;
    padding: 7px 14px;
    border: 1px solid var(--c-accent-glow);
    border-radius: var(--radius-pill);
    background: rgba(10, 18, 38, 0.6);
    backdrop-filter: blur(8px);
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--c-mist-bright);
  }
  .spd-hero__name {
    margin: 28px 0 0;
    max-width: 16ch;
    font-family: var(--font-display);
    font-size: clamp(2.6rem, 6vw, 4.6rem);
    line-height: 1.04;
    font-weight: 600;
    letter-spacing: -0.02em;
    color: var(--c-pearl);
  }
  .spd-hero__tagline {
    margin: 24px 0 0;
    max-width: 46ch;
    font-size: clamp(1.05rem, 1.6vw, 1.3rem);
    line-height: 1.5;
    color: var(--c-mist);
  }

  /* ---------- BLOCCHI CORPO (due colonne) ---------- */
  .spd-block {
    padding: var(--space-section-y) var(--space-section-x);
  }
  .spd-block__inner {
    max-width: var(--container-max);
    margin: 0 auto;
    display: grid;
    grid-template-columns: minmax(0, 5fr) minmax(0, 7fr);
    column-gap: 64px;
    align-items: start;
  }
  .spd-block__title {
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 2.6vw, 2.1rem);
    line-height: 1.22;
    font-weight: 500;
    color: var(--c-pearl);
  }
  .spd-block__body {
    margin: 0;
    font-size: 1rem;
    line-height: 1.7;
    color: var(--c-mist);
  }
  .spd-block__link {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    margin-top: 24px;
    font-family: var(--font-mono);
    font-size: 13px;
    letter-spacing: 0.04em;
    color: var(--c-cyan);
    text-decoration: none;
    transition: color 0.2s var(--ease);
  }
  .spd-block__arrow {
    transition: transform 0.2s var(--ease);
  }
  .spd-block__link:hover {
    color: var(--c-accent-bright);
  }
  .spd-block__link:hover .spd-block__arrow {
    transform: translateX(4px);
  }
  .spd-block__link:focus-visible {
    outline: 2px solid var(--c-cyan);
    outline-offset: 4px;
    border-radius: 2px;
  }

  /* ---------- SIGNATURE: blocco distintivo "edge" ---------- */
  /* Il dimming continuo: fondo più profondo, filo rame. */
  .spd-block--feature .spd-block__inner {
    box-sizing: border-box;
    background: var(--c-onyx);
    border: 1px solid var(--border-hairline);
    border-left: 2px solid var(--c-copper);
    border-radius: var(--radius-card);
    padding: 48px;
    column-gap: 56px;
  }
  .spd-block--feature .spd-block__title {
    color: var(--c-pearl);
  }

  /* ---------- CTA FINALE ---------- */
  .spd-cta {
    padding: var(--space-section-y) var(--space-section-x) var(--space-section-y-lg);
  }
  .spd-cta__inner {
    max-width: 720px;
    margin: 0 auto;
    text-align: center;
  }
  .spd-cta__title {
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(1.6rem, 3vw, 2.4rem);
    line-height: 1.2;
    font-weight: 500;
    color: var(--c-pearl);
  }
  .spd-cta__body {
    margin: 16px auto 0;
    max-width: 52ch;
    color: var(--c-mist);
    line-height: 1.6;
  }
  .spd-cta__button {
    display: inline-block;
    margin-top: 32px;
    padding: 14px 32px;
    border: 1px solid var(--c-cyan);
    border-radius: var(--radius-card);
    background: transparent;
    font-family: var(--font-mono);
    font-size: 13px;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--c-cyan);
    text-decoration: none;
    transition: background 0.2s var(--ease), color 0.2s var(--ease);
  }
  .spd-cta__button:hover {
    background: var(--c-cyan);
    color: var(--c-void);
  }
  .spd-cta__button:focus-visible {
    outline: 2px solid var(--c-cyan);
    outline-offset: 4px;
  }

  /* ---------- RESPONSIVE ---------- */
  @media (max-width: 900px) {
    .spd-hero {
      padding: calc(var(--space-section-y) + 24px) 24px var(--space-section-y);
    }
    .spd-block {
      padding: var(--space-section-y) 24px;
    }
    .spd-block__inner {
      grid-template-columns: 1fr;
      row-gap: 20px;
    }
    .spd-block--feature .spd-block__inner {
      padding: 32px 24px;
    }
  }
  @media (max-width: 560px) {
    .spd-hero {
      padding: 72px 24px 56px;
    }
    .spd-block {
      padding: 56px 24px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .spd-block__link,
    .spd-block__arrow,
    .spd-cta__button {
      transition: none;
    }
  }
</style>
