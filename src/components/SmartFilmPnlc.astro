---
// SmartFilmPnlc.astro — Pagina-prodotto Smart Film PNLC (logica inversa / reverse).
// Struttura: hero prodotto (asimmetrico, sinistra) → 4 blocchi corpo a due colonne
//            (titolo sx / testo dx) → CTA finale. Derivato da BlackoutHyperGlass.astro.
// Il blocco "edge" (distintivo) ha fondo --c-onyx + bordo copper: signature = il
//            fail-safe, il vetro che resta trasparente senza alimentazione.
// Differenza dal template Blackout: il blocco "what" linka alla pagina PDLC
//            (logica diretta) → const pdlcHref lang-aware.
// lang/t ricavati internamente (mai props). Token: --c-* / --space-* / --font-* da global.css.
import { getLangFromUrl, useTranslations } from "../i18n/utils";
const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

// Base URL lang-aware per i link cross-page verso la home (prefixDefaultLocale: false).
const base = lang === "en" ? "/en/" : "/";
const linkHow = `${base}#how-eyebrow`;
const linkBlogic = `${base}#blogic-eyebrow`;

// Link a pagina-prodotto: i segmenti di path sono tradotti → const dedicato lang-aware.
const pdlcHref = lang === "en"
  ? "/en/technologies/smart-film-pdlc"
  : "/tecnologie/smart-film-pdlc";

// Blocchi corpo a due colonne. "edge" è il blocco distintivo (variante visiva).
const blocks = [
  { key: "what", variant: "plain", link: pdlcHref, linkKey: "prod.pnlc.what.link" },
  { key: "edge", variant: "feature", link: null },
  { key: "how", variant: "plain", link: linkHow, linkKey: "prod.pnlc.how.link" },
  { key: "scale", variant: "plain", link: linkBlogic, linkKey: "prod.pnlc.scale.link" },
] as const;
---
<article class="pnlc">
  <!-- HERO PRODOTTO — asimmetrico, allineato a sinistra -->
  <header class="pnlc-hero" aria-labelledby="pnlc-name">
    <div class="pnlc-hero__inner">
      <p class="pnlc-hero__eyebrow">{t("prod.pnlc.eyebrow")}</p>
      <h1 class="pnlc-hero__name" id="pnlc-name">{t("prod.pnlc.name")}</h1>
      <p class="pnlc-hero__tagline">{t("prod.pnlc.tagline")}</p>
    </div>
  </header>

  <!-- BLOCCHI CORPO — due colonne (titolo sx / testo dx) -->
  <div class="pnlc-body">
    {blocks.map((b) => (
      <section class={`pnlc-block pnlc-block--${b.variant}`}>
        <div class="pnlc-block__inner">
          <div class="pnlc-block__head">
            <h2 class="pnlc-block__title">{t(`prod.pnlc.${b.key}.title`)}</h2>
          </div>
          <div class="pnlc-block__content">
            <p class="pnlc-block__body">{t(`prod.pnlc.${b.key}.body`)}</p>
            {b.link && (
              <a class="pnlc-block__link" href={b.link}>
                {t(b.linkKey)}<span class="pnlc-block__arrow" aria-hidden="true">&#8594;</span>
              </a>
            )}
          </div>
        </div>
      </section>
    ))}
  </div>

  <!-- CTA FINALE -->
  <section class="pnlc-cta" aria-labelledby="pnlc-cta-title">
    <div class="pnlc-cta__inner">
      <h2 class="pnlc-cta__title" id="pnlc-cta-title">{t("prod.pnlc.cta.title")}</h2>
      <p class="pnlc-cta__body">{t("prod.pnlc.cta.body")}</p>
      <a class="pnlc-cta__button" href={`${base}#contact-eyebrow`}>{t("prod.pnlc.cta.button")}</a>
    </div>
  </section>
</article>

<style>
  .pnlc {
    display: block;
  }

  /* ---------- HERO PRODOTTO ---------- */
  .pnlc-hero {
    padding: calc(var(--space-section-y-lg) + 40px) var(--space-section-x) 32px;
  }
  .pnlc-hero__inner {
    max-width: var(--container-max);
    margin: 0 auto;
  }
  .pnlc-hero__eyebrow {
    display: inline-block;
    margin: 0;
    padding: 7px 14px;
    border: 1px solid var(--c-accent-glow);
    border-radius: var(--radius-pill);
    background: rgba(10, 18, 38, 0.6);
    backdrop-filter: blur(8px);
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: var(--c-mist-bright);
  }
  .pnlc-hero__name {
    margin: 28px 0 0;
    max-width: 16ch;
    font-family: var(--font-display);
    font-size: clamp(2.6rem, 6vw, 4.6rem);
    line-height: 1.04;
    font-weight: 600;
    letter-spacing: -0.02em;
    color: var(--c-pearl);
  }
  .pnlc-hero__tagline {
    margin: 24px 0 0;
    max-width: 46ch;
    font-size: clamp(1.05rem, 1.6vw, 1.3rem);
    line-height: 1.5;
    color: var(--c-mist);
  }

  /* ---------- BLOCCHI CORPO (due colonne) ---------- */
  .pnlc-block {
    padding: var(--space-section-y) var(--space-section-x);
  }
  .pnlc-block__inner {
    max-width: var(--container-max);
    margin: 0 auto;
    display: grid;
    grid-template-columns: minmax(0, 5fr) minmax(0, 7fr);
    column-gap: 64px;
    align-items: start;
  }
  .pnlc-block__title {
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 2.6vw, 2.1rem);
    line-height: 1.22;
    font-weight: 500;
    color: var(--c-pearl);
  }
  .pnlc-block__body {
    margin: 0;
    font-size: 1rem;
    line-height: 1.7;
    color: var(--c-mist);
  }
  .pnlc-block__link {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    margin-top: 24px;
    font-family: var(--font-mono);
    font-size: 13px;
    letter-spacing: 0.04em;
    color: var(--c-cyan);
    text-decoration: none;
    transition: color 0.2s var(--ease);
  }
  .pnlc-block__arrow {
    transition: transform 0.2s var(--ease);
  }
  .pnlc-block__link:hover {
    color: var(--c-accent-bright);
  }
  .pnlc-block__link:hover .pnlc-block__arrow {
    transform: translateX(4px);
  }
  .pnlc-block__link:focus-visible {
    outline: 2px solid var(--c-cyan);
    outline-offset: 4px;
    border-radius: 2px;
  }

  /* ---------- SIGNATURE: blocco distintivo "edge" ---------- */
  /* Il fail-safe: fondo più profondo, filo rame. */
  .pnlc-block--feature .pnlc-block__inner {
    box-sizing: border-box;
    background: var(--c-onyx);
    border: 1px solid var(--border-hairline);
    border-left: 2px solid var(--c-copper);
    border-radius: var(--radius-card);
    padding: 48px;
    column-gap: 56px;
  }
  .pnlc-block--feature .pnlc-block__title {
    color: var(--c-pearl);
  }

  /* ---------- CTA FINALE ---------- */
  .pnlc-cta {
    padding: var(--space-section-y) var(--space-section-x) var(--space-section-y-lg);
  }
  .pnlc-cta__inner {
    max-width: 720px;
    margin: 0 auto;
    text-align: center;
  }
  .pnlc-cta__title {
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(1.6rem, 3vw, 2.4rem);
    line-height: 1.2;
    font-weight: 500;
    color: var(--c-pearl);
  }
  .pnlc-cta__body {
    margin: 16px auto 0;
    max-width: 52ch;
    color: var(--c-mist);
    line-height: 1.6;
  }
  .pnlc-cta__button {
    display: inline-block;
    margin-top: 32px;
    padding: 14px 32px;
    border: 1px solid var(--c-cyan);
    border-radius: var(--radius-card);
    background: transparent;
    font-family: var(--font-mono);
    font-size: 13px;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--c-cyan);
    text-decoration: none;
    transition: background 0.2s var(--ease), color 0.2s var(--ease);
  }
  .pnlc-cta__button:hover {
    background: var(--c-cyan);
    color: var(--c-void);
  }
  .pnlc-cta__button:focus-visible {
    outline: 2px solid var(--c-cyan);
    outline-offset: 4px;
  }

  /* ---------- RESPONSIVE ---------- */
  @media (max-width: 900px) {
    .pnlc-hero {
      padding: calc(var(--space-section-y) + 24px) 24px var(--space-section-y);
    }
    .pnlc-block {
      padding: var(--space-section-y) 24px;
    }
    .pnlc-block__inner {
      grid-template-columns: 1fr;
      row-gap: 20px;
    }
    .pnlc-block--feature .pnlc-block__inner {
      padding: 32px 24px;
    }
  }
  @media (max-width: 560px) {
    .pnlc-hero {
      padding: 72px 24px 56px;
    }
    .pnlc-block {
      padding: 56px 24px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .pnlc-block__link,
    .pnlc-block__arrow,
    .pnlc-cta__button {
      transition: none;
    }
  }
</style>
